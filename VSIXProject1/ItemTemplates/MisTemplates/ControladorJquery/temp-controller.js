//| Controlador para vista $fileinputname$
//| Notese que el nombre del controlador de la vista debe hacer match con el id de la etiqueta <section>
app.page("$fileinputname$", function () {
    // Aqui se inicializan las variables de la vista
    

    // present the view - load data and show: 
    return function () {
        // Esta función que se retorna del controlador, se ejecuta cada vez que el usuario consulta la vista,
        // esto permite obtener los datos y mostrarlos.



    }
}); 